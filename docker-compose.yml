version: "3.8"

services:
  django:
    container_name: django
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - django:/home/transcendance
    tty: true
    # networks:
    #   - pong
    init: true
  

volumes:
  django:
    driver: local
    driver_opts:
      type: 'none' 
      o: 'bind'
      device: '/Users/joerober/code/tests/ping/git/docker/django/template/volume'

# networks:
#   pong:
#     driver: bridge